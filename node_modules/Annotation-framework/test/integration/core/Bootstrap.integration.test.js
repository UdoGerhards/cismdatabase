const assert = require('assert');
const path = require('path');
const AnnotationRegistry = require('../../../src/core/AnnotationRegistry');
const Bootstrap = require('../../../src/core/Bootstrap');

describe('Bootstrap (Integration)', () => {
  it('should load annotations and plugins', () => {
    Bootstrap.run();

    const all = AnnotationRegistry.getAll();
    assert.ok(all.length > 0, 'Annotations should be registered');
  });
});
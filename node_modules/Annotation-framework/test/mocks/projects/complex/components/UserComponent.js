/**
 * @Component
 */
class UserComponent {
  /**
   * @Qualifier("UserService")
   */
  userService;

  run() {
    const user = this.userService.getUser(1);
    console.log('Fetched user:', user.sql);
  }
}

module.exports = UserComponent;

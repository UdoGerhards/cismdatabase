/**
 * @Component
 */
class UserService {
  /**
   * @Qualifier("Database")
   */
  db;

  getUser(id) {
    return this.db.query(`SELECT * FROM users WHERE id=${id}`);
  }
}

module.exports = UserService;
